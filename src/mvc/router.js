/**
 * module nginx
 */

KM.define(['./state'], function(K, require){

var state = require('./state');

return {

	/**
	 * pathname -> handler: { key:value }
	 */
	route: function(path, handler, rewriteRule){
		
	}
	
};


});


